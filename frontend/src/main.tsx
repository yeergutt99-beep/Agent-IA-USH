import '@fontsource/roboto/300.css'; import '@fontsource/roboto/400.css'; import '@fontsource/roboto/500.css'; import '@fontsource/roboto/700.css';
import { CssBaseline, IconButton, ThemeProvider } from '@mui/material'; import { DarkMode } from '@mui/icons-material';
import React, { useState } from 'react'; import { createRoot } from 'react-dom/client'; import { BrowserRouter, Navigate, Route, Routes } from 'react-router-dom';
import { AuthProvider, useAuth } from './context/AuthContext'; import { Layout } from './components/Layout'; import { Login } from './pages/Login'; import { Dashboard } from './pages/Dashboard'; import { Reservations } from './pages/Reservations'; import { CreateReservation } from './pages/CreateReservation'; import { Calendar, Customers, Settings, Tables } from './pages/SimplePages'; import { makeTheme } from './theme/theme';
function Private(){ const {user}=useAuth(); return user ? <Layout/> : <Navigate to="/login"/>; }
function App(){ const [mode,setMode]=useState<'light'|'dark'>('light'); return <ThemeProvider theme={makeTheme(mode)}><CssBaseline/><IconButton onClick={()=>setMode(mode==='light'?'dark':'light')} sx={{position:'fixed',right:12,bottom:12,zIndex:9}}><DarkMode/></IconButton><AuthProvider><BrowserRouter><Routes><Route path="/login" element={<Login/>}/><Route element={<Private/>}><Route path="/" element={<Dashboard/>}/><Route path="/calendar" element={<Calendar/>}/><Route path="/reservations" element={<Reservations/>}/><Route path="/create" element={<CreateReservation/>}/><Route path="/settings" element={<Settings/>}/><Route path="/tables" element={<Tables/>}/><Route path="/customers" element={<Customers/>}/></Route></Routes></BrowserRouter></AuthProvider></ThemeProvider>; }
createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);
